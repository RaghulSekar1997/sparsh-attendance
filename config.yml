# =============================================================================
# Attendance Automation — Configuration
# =============================================================================
# KEEP THIS FILE PRIVATE — it contains credentials.
# It is listed in .gitignore and should never be committed.

# ── DoLynk Pro login ─────────────────────────────────────────────────────────
dolynk:
  login_url:   "https://pro.dolynkcloud.com/#/login"
  records_url: "https://pro.dolynkcloud.com/#/access/record/list"
  username:    "sparshdesigners.in@gmail.com"
  password:    "admin123"

# ── Google Sheets ─────────────────────────────────────────────────────────────
google:
  credentials_json: "sparsh-attendance-5ee4c1c7e02b.json"   # relative to this file
  sheet_name:       "Sparsh_Salary_Auto V3"
  tab_name:         "Records"

# ── Script behaviour ──────────────────────────────────────────────────────────
settings:
  headless:     true    # false = show browser window (debug mode)
  max_retries:  3       # login retry attempts
  timeout_ms:   30000   # per-operation timeout in milliseconds

# ── Apps Script webhook (fires email report after each upload) ────────────────
# Leave webhook_url blank until you've deployed the web app (see instructions below)
apps_script:
  webhook_url:  "https://script.google.com/macros/s/AKfycbwiJ423a-Vy-H75VLmKobUDWmmTMpaGG5BRNtxZYm2Qe1NijQ-yPLtoQ833Kryj22e_EQ/exec"   # ← paste your deployment URL here
  secret_key:   "sparsh-report-2026"
